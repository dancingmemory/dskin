/**
 * The version of this build. Keep in sync with package.json — bumped on
 * every release so the built-in updater can compare itself against the
 * GitHub release tags.
 */
export const DSKIN_VERSION = '1.0.8'

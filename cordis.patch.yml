# Pixel Whale Party skin bundle patch: inserts its dshClient row into the web
# plugin roster. The bundle follows the official dsh plugin shape — install it
# with `dsh plugin --profile <name> add <path-or-git-url>`.
- insert:
    - id: ui-skin-dskin
      name: 'dskin'
